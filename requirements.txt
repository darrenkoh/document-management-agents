ollama>=0.1.0
pypdf>=3.0.0
python-docx>=1.1.0
Pillow>=10.0.0
pyyaml>=6.0
olefile>=0.46
watchdog>=3.0.0
openai>=1.0.0
# tinydb>=4.8.0  # Replaced with SQLite for better performance
numpy>=1.24.0
flask>=3.0.0
flask-cors>=4.0.0
bootstrap-flask>=1.8.0
pdf2image>=1.17.0

# Vector databases for semantic search
chromadb>=0.4.0
faiss-cpu>=1.7.0

# Machine Learning for dimensionality reduction
scikit-learn>=1.3.0

# React frontend build dependencies (optional, for development)
# These are not needed for runtime, only for building the React app
# nodejs>=18.0.0
# npm>=8.0.0

